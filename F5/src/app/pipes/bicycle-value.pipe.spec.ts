import { BicycleValuePipe } from './bicycle-value.pipe';

describe('BicycleValuePipe', () => {
  it('create an instance', () => {
    const pipe = new BicycleValuePipe();
    expect(pipe).toBeTruthy();
  });
});
